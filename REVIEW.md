# REVIEW.md — AICS 코드 리뷰

현재 구현의 구조적 강점, 개선 포인트, 기술 부채를 정리한 문서입니다.

---

## 잘 된 부분

### 파이프라인 설계
문의 처리 흐름(PII 마스킹 → 분류 → 임베딩 → 유사 검색 → 초안 생성 → 저장 → 알림)이 단방향으로 명확히 분리되어 있어 각 단계를 독립적으로 교체하거나 테스트할 수 있습니다.

### LLM 폴백 전략
`openrouter.ts`에서 무료 모델 4개를 순서대로 시도하고 429/502/503 시 다음 모델로 전환합니다. 비용 없이 서비스를 운영 가능한 구조입니다.

### 로컬 임베딩
`@xenova/transformers`로 임베딩을 서버에서 직접 실행하므로 외부 API 의존 없이 유사 문의 검색이 가능합니다. 네트워크 지연이 없고 비용도 없습니다.

### 타입 일관성
`types/index.ts`에 도메인 타입이 집중 정의되어 있고, 백엔드 전반에서 공유됩니다. `InquiryCategory` union 타입 덕분에 잘못된 카테고리 값이 컴파일 타임에 차단됩니다.

### 도메인 특화 분류
SI/SM 회사의 실제 업무 흐름(계약→견적→개발→유지보수→장애 대응)을 카테고리에 반영하여 LLM 분류 정확도를 높였습니다. 시스템 프롬프트에 각 카테고리 정의를 명시해 모호한 경계를 줄였습니다.

### 고객사·직원 데이터 분리
로그인 계정(`users`)과 직원 디렉토리(`employees`)를 분리해 시스템 접근 권한과 업무 담당 정보를 독립적으로 관리합니다. 개발팀은 담당 업체가 고정 배정되고, 기획팀·디자인팀은 카테고리 기반 알림 설정으로 처리합니다.

---

## 개선 포인트

### 1. 임베딩 초기 지연
첫 요청 시 `all-MiniLM-L6-v2` 모델(~30MB)을 다운로드하므로 콜드 스타트가 수십 초 걸릴 수 있습니다. 서버 시작 시 백그라운드에서 미리 워밍업하는 것이 좋습니다.

```typescript
// server.ts 에서 서버 시작과 동시에 워밍업
import { embed } from './services/embedder';
embed('warmup').catch(() => {});
```

### 2. 초안 생성 병렬 호출의 Rate Limit 위험
`drafter.ts`에서 3개 초안을 `Promise.all`로 동시에 요청합니다. 무료 모델은 동시 요청 제한이 엄격해 일부가 실패할 수 있습니다. 순차 실행 또는 재시도 로직 추가를 고려하세요.

### 3. DB 마이그레이션 관리
현재 `client.ts`에서 `ALTER TABLE`을 `try/catch`로 묻는 방식은 마이그레이션이 늘어날수록 관리가 어렵습니다. `user_version` pragma 기반의 버전 관리나 `better-sqlite3-migrations` 같은 라이브러리 도입을 고려하세요.

### 4. `better-sqlite3`의 동기 I/O
`better-sqlite3`는 동기 API입니다. 현재 규모에서는 문제없지만, 동시 요청이 많아지면 쿼리 중 이벤트 루프가 블로킹됩니다. 장기적으로는 `Drizzle ORM` + `libsql`(비동기 드라이버) 전환을 고려할 수 있습니다.

### 5. Notion 데이터베이스 속성명 하드코딩
`notion.ts`에서 `작업 이름`, `업체명`, `상태`, `담당자` 등 9개 속성명이 한국어로 하드코딩되어 있습니다. 사용자가 다른 이름의 데이터베이스를 연결하면 오류가 발생합니다. 설정 UI에서 속성명을 지정할 수 있게 하면 유연성이 높아집니다.

### 6. PII 마스킹의 한계
현재 정규식 기반 마스킹은 명시적 패턴만 처리합니다. 자연어로 노출된 개인정보(예: "제 이름은 홍길동이고 주소는...")는 감지되지 않습니다. 민감도가 높은 환경이라면 LLM 기반 PII 탐지를 추가하거나, 처리 전 사용자에게 개인정보 미입력을 안내하는 UI 경고가 필요합니다.

### 7. 에러 응답 일관성
라우트마다 에러 핸들링 방식이 다릅니다. Express 전역 에러 미들웨어를 추가하고 `next(err)` 패턴으로 통일하면 유지보수가 쉬워집니다.

### 8. 고객사 선택과 Notion 담당자 배정의 단절
문의 입력 시 고객사를 선택해도, Notion 작업의 담당자는 카테고리별 `notion_user_id` 설정을 따릅니다. 즉 고객사의 개발팀 담당자(이상진, 김희철 등)와 Notion 담당자가 자동 연결되지 않습니다. 향후 `employees` 테이블에 `notion_user_id`를 추가하고, 고객사 담당 직원의 Notion UUID를 직접 사용하는 방식으로 개선할 수 있습니다.

---

## 보안 검토

| 항목 | 상태 | 비고 |
|---|---|---|
| JWT 인증 | 양호 | 모든 보호 라우트에 `authenticate` 미들웨어 적용 |
| 역할 기반 접근 제어 | 양호 | 설정 변경은 `requireRole('admin')` 보호 |
| PII 마스킹 | 부분적 | 패턴 기반, 자연어 노출 미탐지 |
| SQL Injection | 양호 | `better-sqlite3` prepared statement 사용 |
| SSE 토큰 노출 | 주의 | URL query string에 JWT 포함 — 서버 로그에 기록될 수 있음 |
| CORS | 양호 | `FRONTEND_URL` 환경변수로 오리진 제한 |
| Notion API Key | 주의 | 서버 환경변수로 관리됨. `.env`가 버전 관리에 포함되지 않도록 주의 |

---

## 기술 부채 우선순위

| 우선순위 | 항목 |
|---|---|
| 높음 | 임베딩 워밍업 — 사용자 경험 직접 영향 |
| 높음 | 초안 병렬 호출 재시도 — 무료 모델 안정성 문제 |
| 중간 | DB 마이그레이션 버전 관리 체계화 |
| 중간 | Notion 속성명 유연성 |
| 중간 | 고객사 담당자 → Notion 담당자 자동 연결 |
| 낮음 | 에러 핸들링 미들웨어 통일 |
| 낮음 | 비동기 DB 드라이버 전환 |
